# スコープ・要件定義

## 文書の使い方

各要件にはID、優先度、受け入れ条件を付ける。実装や試験ではIDを参照し、変更時は理由、影響、承認者を記録する。

- `Must`: 公開に必須
- `Should`: 原則実装するが、代替策を承認できる
- `Could`: 時間と予算に余裕がある場合

## 前提と暫定値

| 項目 | 暫定値 | 確定条件 |
|---|---|---|
| 同時追跡 | 50セッション | 実地予測と負荷試験で確定 |
| 1セッションの追跡上限 | 6時間 | 展示導線と電池試験で確定 |
| 位置取得間隔 | 5秒を初期値 | 精度・通信量・電池試験で調整 |
| 到着演出 | 15秒を初期値 | 10〜20秒の表現試験で確定 |
| 対応言語 | 日本語、英語 | 来場者要件で確定 |
| 本番配置 | Vercel + Google Cloud東京を第一候補、AWS東京を代替 | 大学の情報・個人情報担当が提供者と保存地域を承認 |
| 本番正本DB | MVPからマネージドPostgreSQL | Cloud SQLまたはRDSのversion、可用性、接続上限を負荷試験と大学承認で確定 |
| 通常GPSの保持 | 生座標は処理中のみ、量子化済み最新値は30〜60秒 | 個人情報責任者が公開前に承認 |
| 写真撮影時GPSの保持 | 写真と同じく24時間以内を提案 | 個人情報責任者が公開前に承認 |
| 1セッションの写真 | 12枚 | 実機の通信量・容量試験で確定 |
| 写真の公開表示 | 行わない | 将来実施時は別同意とモデレーションを設計 |

## 対象範囲

### 対象

- QRからの参加、説明、同意、位置権限取得
- Web App内での写真撮影、縮小・EXIF除去、本人用写真ストーリー
- 追跡状態表示、停止、即時削除
- 通信できた最新位置の検証、短期保持、座標変換、抽象化
- キャンパス経路グラフと、写真間の経路再構成・信頼度判定
- 現在位置と到着軌跡のリアルタイム描画
- 到着キューと運営操作
- 監視、障害復旧、合成データへのフォールバック

### 対象外

- 個人アカウント、SNSログイン、ランキング
- 正確な屋内階層判定
- 行動分析や個人プロファイリング
- 無期限の軌跡アーカイブ
- 参加端末への専用アプリのインストール
- 写真原画像の公開スクリーン表示、友人間共有、SNS投稿
- 自動Vlog動画生成、写真内容の人物・文字・物体分析

## 機能要件

| ID | 優先 | 要件 | 受け入れ条件 |
|---|---|---|---|
| FR-001 | Must | 写真・位置の取得目的、項目、表示方法、保存期間、停止・削除方法、問い合わせ先を権限要求前に表示する | 未同意ではカメラ・位置APIを呼ばずセッションを開始しない |
| FR-002 | Must | サーバーが有効な同意文バージョンを配信する | 旧版・未知の版によるセッション作成を拒否する |
| FR-003 | Must | MemorySessionとLivePresenceSessionを別ID・別トークン・別同意で発行する | 一方のトークンで他方の写真・位置・削除APIを操作できない |
| FR-004 | Must | 参加中画面に追跡状態と最終送信時刻を表示する | 位置更新停止を30秒以内に参加者が認識できる |
| FR-005 | Must | 有効な位置点を指定間隔で送信する | 位置権限許可時に対象端末で有効点を取得できる |
| FR-006 | Must | 通常GPSは通信できた最新1件だけを送り、通信断中の履歴を保存・再送しない | 復旧後に過去点のバッチ送信がなく、最新点だけが反映される |
| FR-007 | Must | 参加者がいつでも追跡を停止できる | 停止受付後の位置点を保存しない |
| FR-008 | Must | 写真の思い出、リアルタイム参加、両方を分けて削除できる | 選んだ範囲だけを削除し、両方削除ではExperienceLinkも残らない |
| FR-009 | Must | 到着地点の有効なQR/NFCだけで到着を受け付ける | 期限切れ、改変、別会場の到着証明を拒否する |
| FR-010 | Must | 到着時に`arrived`へ移し、停止または追加同意による`on_site`継続を選べる | 到着だけで会場内用途へ変更せず、追加同意なしに`on_site`表示しない |
| FR-011 | Must | 到着後に待ち状態と予想順を参加端末へ表示する | 再接続後も到着・再生状態を復元できる |
| FR-012 | Must | 参加端末と展示に同じ非識別の色・記号を表示する | 参加者がセッションIDを見ず自分の演出を識別できる |
| FR-013 | Must | 軌跡再生をスキップして終了できる | スキップ後に待ち行列から除外される |
| FR-014 | Must | 到着キューを一度に一つの主描画機が処理する | 二重再生が発生しない |
| FR-015 | Must | 運営者が受付停止、再生スキップ、フォールバック切替を行える | 権限のある運営端末だけが操作でき履歴が残る |
| FR-016 | Must | 描画接続時にスナップショットから状態を復元する | 再起動後、目標時間内に現在表示へ復帰する |
| FR-017 | Must | GPS・通信・サーバー障害時に合成データ表現へ切り替える | 判断後1分以内に展示を継続できる |
| FR-018 | Should | QRチェックポイントで位置取得不能時の代替軌跡を作る | GPSを許可しなくても代替参加を完了できる |
| FR-019 | Should | 日本語と英語を切り替える | 同意内容と主要操作が両言語で一致する |
| FR-020 | Could | NFC到着に対応する | QRと同じ到着検証・冪等性を満たす |
| FR-021 | Must | Web App内で写真を撮影し、撮影時刻と撮影時GPSを別々に記録する | EXIF位置に依存せずPhotoMomentを作成できる |
| FR-022 | Must | 送信前に画像の向きを正規化し、EXIF除去、縮小、再エンコードを行う | 保存画像からEXIFと元解像度を取得できない |
| FR-023 | Must | 写真送信の進行、成功、失敗、再送状態を表示する | 通信断後に上限内で重複なく再送できる |
| FR-024 | Must | 連続する写真撮影時GPSをキャンパス経路グラフへ合わせ、歩行可能な最短経路を再構成する | 通常GPS点列を使わず、通行不能な辺を通常経路として採用しない |
| FR-025 | Must | 再構成区間へ信頼度を付ける | 不確かな区間を正確な実測軌跡として表示しない |
| FR-026 | Must | 到着後に本人だけが写真ストーリーを閲覧できる | 参加トークンなしで写真や縮小画像を取得できない |
| FR-027 | Must | 写真単位とセッション全体で削除できる | 撮影画像、加工画像、派生特徴、端末キャッシュ、経路との参照を削除する |
| FR-028 | Must | 公開スクリーンには原写真を送信・表示しない | 描画イベントと表示端末の通信に画像URL・画像バイト列がない |
| FR-029 | Must | カメラ拒否・写真0枚でも抽象軌跡へ参加できる | 写真撮影を強制せず到着・終了まで進める |
| FR-030 | Should | 本人端末へ写真を保存できる | 保存成功後もサーバー保持期限は延長しない |
| FR-031 | Must | 写真保存完了時に量子化済み撮影地点を3D Mapへ15〜30分以内のピンとして表示する | 描画イベントに写真、URL、生GPS、正確な時刻、セッションIDがない |
| FR-032 | Must | 同じ空間bucketの有効撮影が3件以上の場合だけフォトスポットを表示する | 削除・期限切れで3件未満になれば表示を止める |
| FR-033 | Must | 少人数時に個別写真ピンを遅延・追加量子化・短縮・非表示にできる | 設定した閾値の上下で期待する保護モードへ切り替わる |
| FR-034 | Should | PWA利用者へ15分または30分間隔の任意Web Push通知を提供する | 明示購読後だけ配信し、到着・停止・解除・期限で終了する |
| FR-035 | Must | 座標変換、CampusGraph、3D Mapの互換バージョンをイベントへ付ける | 不一致イベントを描画せず、直前版へ戻せる |
| FR-036 | Must | `on_site`で点・エリア・操作時のみの表示を切り替えられる | 屋内精度が基準未達ならエリアまたは操作時表示で継続できる |

## 非機能要件

| ID | 優先 | 要件 | 受け入れ条件 |
|---|---|---|---|
| NFR-001 | Must | 最大50追跡、60相当の負荷に耐える | 30分試験でエラー率1%未満、データ欠落1%未満 |
| NFR-002 | Must | 有効位置受信後の展示反映を速くする | p95で2秒以内。端末の取得間隔は含めない |
| NFR-003 | Must | 到着から再生開始までの待ち時間を抑える | 通常負荷でp95 30秒以内 |
| NFR-004 | Must | 描画切断から自動復旧する | ネットワーク復旧後10秒以内に再接続を開始する |
| NFR-005 | Must | 8時間連続稼働する | メモリ枯渇、手動再起動、表示停止がない |
| NFR-006 | Must | 通信をTLSで保護する | 公開経路がHTTPS/WSSのみである |
| NFR-007 | Must | 生位置を描画・運営ログへ渡さない | 通信・ログ検査で緯度経度が検出されない |
| NFR-008 | Must | 対象ブラウザを明示する | 試験計画の必須端末ですべてMust要件に合格する |
| NFR-009 | Must | アクセシブルな主要操作を提供する | キーボード、読み上げ、200%拡大で開始・停止・削除可能 |
| NFR-010 | Should | 1セッションの通信量と電池消費を抑える | 実地試験結果を記録し、参加前の注意へ反映する |
| NFR-011 | Must | 写真保存を安定して処理する | 対応端末・回線で保存成功率99%以上 |
| NFR-012 | Must | 写真閲覧URLを公開しない | 短時間の署名付きアクセスと参加認証でのみ取得できる |
| NFR-013 | Must | 写真を含むセッション全体を期限削除する | オブジェクトストレージを含む期限削除試験に合格する |
| NFR-014 | Should | 写真間の再構成経路を速く生成する | PhotoMoment確定後p95 3秒以内にRouteSegmentが利用可能になる |
| NFR-015 | Must | 本番の正本DBにマネージドPostgreSQLを使用する | 再起動・再配置後もデータを失わず、バックアップ復旧と期限削除の試験に合格する |
| NFR-016 | Must | Go Backendを提供者へ依存しないコンテナとして構築する | 同じcontainer imageと標準PostgreSQL schemaをGCPまたはAWSの承認済み環境へ配置できる |
| NFR-017 | Must | DB接続数を接続プールとBackend最大インスタンス数で制御する | 最大負荷時もDB接続上限の予約枠を消費せず、接続枯渇が発生しない |
| NFR-018 | Must | local、staging、productionを分離する | 本番資格情報・参加者データがlocalまたはstagingへコピーされない |

## プライバシー・セキュリティ要件

| ID | 優先 | 要件 | 受け入れ条件 |
|---|---|---|---|
| PS-001 | Must | 氏名、学籍番号、メール、広告ID、端末固有IDを収集しない | DB、ログ、解析ツールの項目監査に合格する |
| PS-002 | Must | キャンパス外の位置点を保存しない | 境界内外の試験点で期待どおり判定する |
| PS-003 | Must | 表示位置を量子化・オフセットする | 描画出力から生座標を復元できない設定である |
| PS-004 | Must | データ種別ごとの削除期限を自動適用する | 期限超過データの削除ジョブ試験に合格する |
| PS-005 | Must | 到着証明の偽造・再利用を防ぐ | 改変、期限切れ、他会場、再送の試験に合格する |
| PS-006 | Must | 参加・描画・運営の認証を分離する | 各資格情報で範囲外APIを実行できない |
| PS-007 | Must | レート制限と最大入力サイズを設ける | 過剰送信が429または接続制限になる |
| PS-008 | Must | 実データを開発・デモへ転用しない | フォールバックと試験が合成データで動く |
| PS-009 | Must | 写真原画像を公開スクリーン、運営画面、ログへ出さない | 通信・画面・ログ検査に合格する |
| PS-010 | Must | 写真オブジェクトを非公開で保存する | 公開URL、一覧取得、推測可能なキーでアクセスできない |
| PS-011 | Must | 写真の第三者写り込みと撮影禁止情報について事前案内する | 撮影画面から常にルールを確認できる |
| PS-012 | Must | 写真削除時に縮小画像、派生色、キャッシュも削除する | 削除後の全保存先検査に合格する |
| PS-013 | Must | 通常GPSの生座標と履歴をDB、ログ、端末再送キューへ残さない | 通信断・再接続・ログ検査で過去点が検出されない |
| PS-014 | Must | 写真ピンとフォトスポットから個人写真・正確な行動を推測しにくくする | 量子化、TTL、3件閾値、少人数モードの試験に合格する |

## 運用要件

| ID | 優先 | 要件 | 受け入れ条件 |
|---|---|---|---|
| OPS-001 | Must | 開場・閉場チェックリストを運営者が実施する | 実施者、時刻、結果が残る |
| OPS-002 | Must | 当日責任者と障害連絡網を定める | 運営場所から参照できる |
| OPS-003 | Must | リアルタイム受付を即時停止できる | 操作後、新規セッションを拒否し案内を表示する |
| OPS-004 | Must | バックアップ描画と合成映像を用意する | 本番機材で切替リハーサルに合格する |
| OPS-005 | Must | 終了時に追跡停止と削除ジョブを確認する | 閉場手順に記録が残る |
| OPS-006 | Must | 本番公開を自動pushだけで行わず、承認済み成果物を昇格する | 技術責任者の承認、release manifest、直前版へのロールバック結果が残る |
| OPS-007 | Must | DB schema migrationをGitで版管理し、後方互換を保って適用する | stagingでmigration・rollback範囲を試験し、production起動時に破壊的migrationを自動実行しない |

## 変更管理

要件変更時は、変更理由、影響する要件ID、データ・同意文への影響、試験変更、承認者をプルリクエストまたは決定記録に残す。Must要件を削除または緩和する場合は、展示責任者と該当領域の責任者による承認を必要とする。
