# 写真メモリー・経路再構成仕様

## 目的

参加者がキャンパスで心に留まった瞬間を写真として残し、その写真と写真の間を歩いた記憶を光の経路として再構成する。正確なGPSログを見せることより、「何を見て、どのように次の瞬間へ移ったか」を本人が振り返れることを優先する。

写真は思い出の主役、写真撮影時GPSは写真間の経路を推定するアンカーとして扱う。通常のリアルタイムGPSは経路再構成へ流用しない。

## MVPの公開範囲

- 参加者はWeb App内で写真を撮影する
- 写真ごとに撮影時刻、撮影時のGPS、精度を記録する
- 連続する写真の撮影時GPSを端点にし、キャンパス経路グラフ上で最短の歩行可能経路を再構成する
- 写真保存完了時に、量子化した撮影地点を3D Mapへ短時間の個別ピンとして表示できる
- 同じ空間bucketに有効な撮影が3件以上ある場合だけ、フォトスポットとして集合表示する
- 到着後、本人のスマホで写真と経路を時系列に見返せる
- 公開スクリーンには写真原画像を表示せず、抽象化した色、光、経路、撮影瞬間を表示する
- 写真の公開共有、友人間共有、自動Vlog動画生成はMVP対象外とする

将来、写真を公開スクリーンへ出す場合は、位置情報への同意と分離した写真単位の明示同意、投稿前確認、モデレーション、撤回、第三者の写り込み対策を追加する。

## 参加体験

1. 作品説明、位置・写真の扱い、保存期間を確認して参加する
2. 位置情報とカメラの権限を許可する
3. 最初の写真を撮り、最初の`PhotoMoment`を作る
4. キャンパスを歩き、残したい瞬間を自由に撮る
5. 写真を撮るたびに前回写真との`RouteSegment`を生成する
6. 展示場へ到着すると、最後の写真から到着点までを補完する。リアルタイム参加は別セッションとして停止または会場内継続を選ぶ
7. 公開スクリーンで抽象化された軌跡演出を見る
8. 本人のスマホで写真ストーリーを見返し、保存または削除する

写真が0枚でも通常の抽象軌跡として参加できる。1枚の場合は一つの記憶として表示し、2枚以上の場合に写真間の経路を持つストーリーを生成する。

## 撮影時の処理

撮影ボタンを押した時点で、カメラ画像とは別にブラウザの位置APIから現在位置を取得する。写真EXIFの位置情報を正としない。

```text
撮影操作
  -> 撮影時位置を取得
  -> 画像の向きを正規化
  -> EXIFを除去
  -> 長辺1920px以下へ縮小
  -> JPEG/WebPへ再エンコード
  -> PhotoMomentと一緒に送信
  -> サーバー保存後に端末内送信待ちデータを削除
```

暫定制限:

| 項目 | MVP上限 |
|---|---:|
| 1セッションの写真 | 12枚 |
| 画像送信サイズ | 1枚5MB以下 |
| 保存後の長辺 | 1920px以下 |
| 撮影位置の取得待ち | 10秒 |
| オフライン送信待ち | 12枚、合計30MB以下 |

撮影位置が取得できない場合も写真は保存できる。通常GPSから位置を補完せず、経路上に配置しない「場所不明の記憶」として残す。

## キャンパス経路グラフ

キャンパス内の歩行可能な経路をグラフ`G = (V, E)`として管理する。

### CampusNode

- 交差点
- 建物・展示場の入口
- 階段、エレベーター、スロープ
- 広場や通路の端点
- キャリブレーション基準点

### CampusEdge

- 始点・終点
- 形状を表す中間座標列
- 距離
- 屋内・屋外
- 階段、スロープ、エレベーター等の属性
- 一時通行止め、利用時間、立入禁止
- バリアフリー経路の可否

写真GPSを単純に最寄り頂点へ移さず、原則として最寄りの通行可能な辺上へ投影する。GPS精度円内に複数の辺がある場合は、前後の写真アンカー、時間差、辺の属性を使って候補を評価する。

## 経路再構成

写真`n-1`から写真`n`の区間について、次の入力を使う。

- 両写真の撮影時GPS、精度、時刻
- キャンパス経路グラフのバージョン
- 通行止め、階段、屋内外などの経路属性

候補経路はA*またはDijkstraで求める。距離だけの最短経路ではなく、次の評価値を最小化する。

```text
routeScore =
  pathLength
  + endpointSnapWeight * endpointSnapDistance
  + uncertaintyWeight * lowAccuracyPenalty
  + restrictedEdgePenalty
  + unnaturalTurnPenalty
```

- 写真アンカーが二つある: 両端を接続する最短の歩行可能経路を基本とする
- 同程度の候補が複数ある: 時間差、曲がり方、辺属性を補助に使い、信頼度を下げる
- GPS精度が悪い: 複数候補の差が小さい場合、信頼度を下げる
- グラフへ安全に吸着できない: 無理に道へ寄せず、不確かな光として表現する
- 通行不可能な辺しか候補にない: 区間を未確定にし、直線を実歩行として扱わない

生成した経路は「実測軌跡」ではなく「GPSと歩行可能経路から再構成した軌跡」と呼ぶ。

## 信頼度

各`RouteSegment`へ`high`、`medium`、`low`の信頼度を付ける。

| 信頼度 | 暫定条件 | 表現 |
|---|---|---|
| high | 両端精度30m以内で候補がほぼ一意 | 明確な光の経路 |
| medium | 両端精度が許容内だが複数候補が近い | 柔らかい線、広い光 |
| low | 端点精度が悪い、グラフ外、候補を決められない | 点線、霧、写真間の余白 |

信頼度判定の数値は実地試験で確定する。不確かな経路を正確に歩いた道として表示しない。

## 写真ストーリー

本人の参加画面では、時系列に次を表示する。

- 写真
- 撮影時刻。秒単位の正確な時刻を公開共有しない
- 前の写真からの再構成経路
- 経過時間
- 経路の不確実さ
- 写真単位の削除とセッション全体の削除

MVPではWeb上のストーリー表示と、本人端末への写真保存を提供する。動画書き出し、音楽付与、友人との共同ストーリーは将来機能とする。

## 公開スクリーンの表現

- 写真撮影時に光が生まれる
- 写真の主要色など、復元性の低い特徴だけを表現へ使える
- 写真原画像、顔、文字、EXIF、正確な撮影位置を表示しない
- 写真間の経路を10〜20秒へ圧縮して再生する
- 信頼度を線の太さ、ぼかし、粒子の散らばりへ反映する
- 撮影枚数や撮影頻度を優劣・ランキングとして扱わない

### リアルタイム写真ピン・フォトスポット

- `PhotoMoment`が`ready`になった時だけ、座標変換済み・5〜10m量子化済みのピンイベントを送る
- ピンには写真、画像URL、生GPS、正確な時刻、MemorySession IDを含めない
- 発生アニメーションは数秒、残留表示は15〜30分以内とする
- 同じ空間bucketに有効な撮影が3件以上ある場合だけフォトスポットを表示する
- 少人数時は遅延、追加量子化、残留時間短縮、非表示のいずれかを自動適用する
- 写真削除時はピンを消し、集計を減算する。閾値未満になったフォトスポットも消す
- 運営は写真ピンとフォトスポットを別々に全停止できる

## 任意の撮影リマインダー

PWAをホーム画面へ追加した参加者を主対象に、Web Pushで15分または30分ごとの撮影リマインダーを任意提供する。通知許可は作品説明直後ではなく、最初の撮影など価値を理解した後に求める。通知時刻はOS・ブラウザ都合で遅延するため厳密なタイマーとして扱わず、画面内タイマーを補助にする。到着、参加停止、購読解除、期限切れで配信を終え、本文に位置・写真・参加記号を含めない。通知利用率はMVPの公開可否条件にしない。

## 写真の安全・プライバシー

- 撮影前に、他人の顔、名札、個人情報、授業資料、撮影禁止場所を写さないよう案内する
- Web Appで画像を再エンコードし、EXIFを送信・保存しない
- 写真は推測困難なオブジェクトキーで保存し、公開URLを持たせない
- 取得には参加トークンと短時間の署名付きURLを必要とする
- CDNや画像変換サービスのアクセスログへ参加トークンや位置を含めない
- 加工済み写真の別系統バックアップとオブジェクトバージョニングは原則無効にする
- 削除時は写真、縮小画像、派生特徴、送信待ちキャッシュを対象にする
- 自動顔検出は補助機能であり、撮影ルールや公開制限の代替にしない

## 障害時

- カメラ拒否: 写真なしの抽象軌跡として参加できる
- 位置拒否: 写真だけの時系列ストーリーとして参加できる
- 写真送信失敗: 上限付きで端末へ保持し、再送または本人端末への保存を案内する
- 経路生成失敗: 写真は残し、写真間を未確定区間として表現する
- 写真削除失敗: 新規取得を停止し、運営・個人情報責任者へ通知する
